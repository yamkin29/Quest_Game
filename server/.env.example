# PostgreSQL container settings for local development.
# Copy this file to server/.env and adjust the values.
POSTGRES_USER=quest
# Generate a random one with: openssl rand -base64 24
POSTGRES_PASSWORD=change-me
POSTGRES_DB=quest_game
# Host port for PostgreSQL: 5432 is occupied by another project on this machine
POSTGRES_PORT=5433
